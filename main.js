function calculateAvg(){
    var table = document.getElementById("table-results");
    for(var i; i<table.length; i++){
        var rowTotal = 0;
        for(var j; j<table.length; j++){
            if(table.rows[i].cells[j].className === 'table-result' && table.rows[i].cells[j].innerHTML !== '-'){
                rowTotal +=
            }
        }
    }
}